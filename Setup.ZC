DocClear;
"\nInstall Additional Palettes for ZealOS ";

if (YorN)
{
	CopyTree("ZOSAddPalettes", "~/ZOSAddPalettes");
	"\n\nYou can now import them manually and set them with : PaletteSetNameOfPalette;\n";
}
else
	"\nSetup aborted !\n";
